pipeline {
    agent any

    options {
        timestamps()
    }

    stages {
        stage('Init') {
            steps {
                echo 'okok'
            }
        }
    }
    
    post {
        always {
            deleteDir()
        }
    }
}
